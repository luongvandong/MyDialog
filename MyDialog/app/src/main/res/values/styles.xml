<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo.Light" />

    <style name="dialog_theme" parent="@android:style/Theme.Holo" />

</resources>
